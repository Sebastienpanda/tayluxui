{
	"name": "@taylux/core",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite build --watch",
		"lint": "eslint \"src/**/*.{ts,tsx}\" --fix",
		"test": "vitest run",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist .turbo"
	}
}

